# is113projt3
A dynamic, data-driven web application that leverages the core back-end technologies (Node.js, Express.js, and MongoDB). Design and implement a functioning back-end using routes, connect to a database, and render user-facing pages dynamically with authentication and security features.
